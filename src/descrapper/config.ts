const c = {
  // scripingName: 'ciclounicoUNITN',
  unicode: 'unibs',
  type: 'triennale',
  url: '/corsi/2025?gruppo=1617109934164',
  numberOfYears: 5
}

// export const config = { ...c };
export const config = { ...c, scripingName: `${c.unicode}-${c.type}` };